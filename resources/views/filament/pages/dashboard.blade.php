<x-filament-panels::page>
    @php($isStaff = $mode === 'staff')

    <div class="pm-dashboard pm-dashboard-v5" x-data="dashboardAttendance()" x-init="loadStatus()">
        <header class="pm-dashboard-header">
            <div class="pm-dashboard-heading">
                <p class="pm-eyebrow">{{ $isStaff ? 'My workspace' : 'Command center' }}</p>
                <h1>{{ $isStaff ? 'Good morning, ' . auth()->user()->name : 'Good morning, ' . auth()->user()->name }} <span aria-hidden="true">👋</span></h1>
                <p>{{ $isStaff ? 'Here’s what needs your attention today.' : 'A live view of people, delivery and attendance across your workspace.' }}</p>
            </div>
            <div class="pm-header-actions">
                @if (! $isStaff)
                    @if (auth()->user()->can('manage-tasks'))
                        <a class="pm-dashboard-action pm-dashboard-action-secondary" href="{{ \App\Filament\Resources\TaskResource::getUrl('index') }}">Manage tasks <span>→</span></a>
                        <a class="pm-dashboard-action" href="{{ \App\Filament\Resources\TaskResource::getUrl('create') }}">New task <span>+</span></a>
                    @endif
                @endif
                <div class="pm-header-date"><span class="pm-date-dot"></span>{{ now()->format('l, F j') }}</div>
            </div>
        </header>

        @if ($isStaff)
            <section class="pm-attendance-hero" :class="clockedIn ? 'is-working' : ''">
                <div class="pm-attendance-copy"><div class="pm-status-line"><span class="pm-live-dot" :class="clockedIn ? 'is-live' : ''"></span><span x-text="clockedIn ? 'YOU ARE WORKING' : 'TODAY’S ATTENDANCE'"></span></div><h2 x-text="clockedIn ? 'You’re clocked in' : 'Start your workday'">Start your workday</h2><p x-text="clockedIn ? ('Clocked in at ' + clockInTime) : 'Clock in to start tracking your attendance securely.'">Clock in to start tracking your attendance securely.</p><div class="pm-attendance-meta"><span><strong x-text="clockedIn ? elapsed : '—'">—</strong><small>worked today</small></span><span><strong x-text="branchName">{{ auth()->user()->primaryBranch?->name ?? 'Primary branch' }}</strong><small>assigned branch</small></span></div></div>
                <div class="pm-attendance-action"><button type="button" class="pm-clock-button" @click="submit(clockedIn ? 'clock-out' : 'clock-in')" :disabled="busy"><span class="pm-clock-icon" aria-hidden="true">◷</span><span x-text="busy ? 'Verifying location…' : (clockedIn ? 'Clock out' : 'Clock in')">Clock in</span><span class="pm-button-arrow" aria-hidden="true">→</span></button><small><span aria-hidden="true">⌖</span> Location verification required</small></div>
            </section>
            <div x-show="error" x-cloak class="pm-inline-error" x-text="error"></div>
            <section class="pm-kpi-grid"><a class="pm-kpi" href="{{ url('/staff/tasks') }}"><span class="pm-kpi-label">Open tasks</span><strong>{{ $taskCount }}</strong><span class="pm-kpi-note">{{ $completedCount }} completed · View tasks →</span></a><a class="pm-kpi" href="{{ url('/staff/tasks') }}"><span class="pm-kpi-label">Due soon</span><strong>{{ $dueSoonCount }}</strong><span class="pm-kpi-note">Next 48 hours · Review →</span></a><a class="pm-kpi" href="{{ url('/staff/attendance') }}"><span class="pm-kpi-label">Attendance</span><strong x-text="clockedIn ? 'Active' : 'Ready'">Ready</strong><span class="pm-kpi-note">Secure location check · History →</span></a></section>
            <div class="pm-content-grid"><section class="pm-panel pm-tasks-panel"><div class="pm-panel-heading"><div><p class="pm-eyebrow">Priority queue</p><h2>My tasks</h2></div><a href="{{ url('/staff/tasks') }}">View all <span>→</span></a></div><div class="pm-task-list">@forelse ($tasks as $task)<div class="pm-task-row"><div class="pm-task-marker {{ $task->status === 'done' ? 'done' : '' }}"></div><div class="pm-task-main"><strong>{{ $task->title }}</strong><span>{{ $task->deadline ? 'Due ' . $task->deadline->format('M j, Y') : 'No deadline' }}</span></div><span class="pm-priority priority-{{ $task->priority }}">{{ ucfirst($task->priority) }}</span></div>@empty<div class="pm-empty"><strong>No tasks assigned</strong><span>You’re all clear for now.</span></div>@endforelse</div></section><section class="pm-panel pm-focus-panel"><p class="pm-eyebrow">Today at a glance</p><h2>Keep your momentum.</h2><p>Stay on top of your tasks and keep attendance accurate throughout the day.</p><div class="pm-focus-rule"></div><a class="pm-text-action" href="{{ url('/staff/attendance') }}">Open attendance history <span>→</span></a></section></div>
        @else
            <section class="pm-admin-kpi-grid">
                <a class="pm-kpi pm-kpi-primary" href="{{ url('/admin/users') }}"><span class="pm-kpi-label">Active people</span><strong>{{ $userCount }}</strong><span class="pm-kpi-note">People currently active · Manage people →</span><span class="pm-kpi-trend">People</span></a>
                <a class="pm-kpi" href="{{ \App\Filament\Resources\TaskResource::getUrl('index') }}"><span class="pm-kpi-label">Open tasks</span><strong>{{ $taskCount }}</strong><span class="pm-kpi-note">Work still in progress · Manage tasks →</span></a>
                <a class="pm-kpi" href="{{ \App\Filament\Resources\TaskResource::getUrl('index') }}"><span class="pm-kpi-label">Overdue</span><strong>{{ $overdueCount }}</strong><span class="pm-kpi-note">Requires attention · Review tasks →</span></a>
                <a class="pm-kpi" href="{{ url('/admin/attendance') }}"><span class="pm-kpi-label">Present today</span><strong>{{ $presentCount }}</strong><span class="pm-kpi-note">Attendance check-ins · Open attendance →</span></a>
            </section>
            <section class="pm-analytics-grid"><article class="pm-panel pm-chart-panel pm-attendance-chart"><div class="pm-chart-heading"><div><p class="pm-eyebrow">Attendance</p><h2>Weekly attendance</h2><span>Check-ins over the last 7 days</span></div><a href="{{ \App\Filament\Pages\AttendanceReports::getUrl() }}">Full report <span>↗</span></a></div><div class="pm-bar-chart" role="img" aria-label="Weekly attendance chart">@php($maxAttendance = max(1, collect($attendanceTrend)->max('count')))@foreach ($attendanceTrend as $day)<div class="pm-bar-item"><div class="pm-bar-value">{{ $day['count'] }}</div><div class="pm-bar-track"><div class="pm-bar-fill" style="height: {{ max(8, ($day['count'] / $maxAttendance) * 100) }}%"></div></div><strong>{{ $day['label'] }}</strong><small>{{ $day['date'] }}</small></div>@endforeach</div></article><article class="pm-panel pm-chart-panel pm-pipeline-chart"><div class="pm-chart-heading"><div><p class="pm-eyebrow">Delivery</p><h2>Task pipeline</h2><span>Current work by status</span></div><a href="{{ \App\Filament\Resources\TaskResource::getUrl('board') }}">Open board <span>↗</span></a></div>@php($pipelineTotal = collect($taskPipeline)->sum('count'))<div class="pm-pipeline-list">@foreach ($taskPipeline as $item)<div class="pm-pipeline-row"><div class="pm-pipeline-label"><span>{{ $item['label'] }}</span><strong>{{ $item['count'] }}</strong></div><div class="pm-pipeline-track"><span style="width: {{ $pipelineTotal > 0 ? ($item['count'] / $pipelineTotal) * 100 : 0 }}%"></span></div></div>@endforeach</div><div class="pm-pipeline-total"><strong>{{ $pipelineTotal }}</strong><span>Total tracked tasks</span></div></article></section>
            <section class="pm-report-strip"><div><span class="pm-report-icon">▦</span><div><strong>Attendance & workforce reports</strong><span>Review employee attendance, late arrivals, working hours and branch performance.</span></div></div><a href="{{ \App\Filament\Pages\AttendanceReports::getUrl() }}">Open reporting workspace <span>→</span></a></section>
            <div class="pm-content-grid admin-grid"><section class="pm-panel pm-tasks-panel"><div class="pm-panel-heading"><div><p class="pm-eyebrow">Workspace activity</p><h2>Recent tasks</h2></div><a href="{{ \App\Filament\Resources\TaskResource::getUrl('index') }}">View all <span>→</span></a></div><div class="pm-task-list">@forelse ($recentTasks as $task)<div class="pm-task-row"><div class="pm-task-marker {{ $task->status === 'done' ? 'done' : '' }}"></div><div class="pm-task-main"><strong>{{ $task->title }}</strong><span>{{ $task->assignee?->name ?? 'Unassigned' }}</span></div><span class="pm-status-chip">{{ str_replace('_', ' ', ucfirst($task->status)) }}</span></div>@empty<div class="pm-empty"><strong>No recent tasks</strong><span>Task activity will appear here.</span></div>@endforelse</div></section><section class="pm-panel pm-focus-panel"><p class="pm-eyebrow">Management shortcuts</p><h2>Move work forward.</h2><p>Go directly to the areas that need your attention.</p><div class="pm-quick-links">@can('manage-users')<a href="{{ url('/admin/users') }}">People <span>→</span></a>@endcan<a href="{{ url('/admin/attendance') }}">Attendance <span>→</span></a><a href="{{ \App\Filament\Resources\TaskResource::getUrl('board') }}">Task board <span>→</span></a>@can('view-reports')<a href="{{ \App\Filament\Pages\AttendanceReports::getUrl() }}">Reports <span>→</span></a>@endcan</div></section></div>
        @endif
    </div>
    <style>
        .pm-dashboard-v5 .pm-dashboard-action-secondary{background:#fff;color:#344767!important;border:1px solid #d0d5dd}.pm-dashboard-v5 .pm-dashboard-action-secondary:hover{background:#f9fafb}.pm-dashboard-v5 .pm-kpi{display:flex;cursor:pointer;transition:transform .16s ease,box-shadow .16s ease,border-color .16s ease}.pm-dashboard-v5 .pm-kpi:hover{transform:translateY(-2px);box-shadow:0 8px 24px rgba(16,24,40,.08);border-color:#d0d5dd}.pm-dashboard-v5 .pm-kpi-note{line-height:1.5}.pm-dashboard-v5 .pm-panel-heading a,.pm-dashboard-v5 .pm-chart-heading a,.pm-dashboard-v5 .pm-text-action{white-space:nowrap}.pm-dashboard-v5 .pm-pipeline-total strong{min-width:1ch}.pm-dashboard-v5 .pm-quick-links a{min-height:48px;display:flex;align-items:center;justify-content:space-between;padding:0 2px;border-bottom:1px solid #eef0f3}.pm-dashboard-v5 .pm-quick-links a:last-child{border-bottom:0}.pm-dashboard-v5 .pm-task-row{cursor:default}@media(max-width:900px){.pm-dashboard-v5 .pm-admin-kpi-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.pm-dashboard-v5 .pm-dashboard-header{align-items:flex-start;flex-direction:column}.pm-dashboard-v5 .pm-header-actions{width:100%;justify-content:flex-start}}@media(max-width:560px){.pm-dashboard-v5 .pm-admin-kpi-grid,.pm-dashboard-v5 .pm-kpi-grid{grid-template-columns:1fr}.pm-dashboard-v5 .pm-dashboard-header h1{font-size:24px!important}.pm-dashboard-v5 .pm-header-actions{gap:8px}.pm-dashboard-v5 .pm-dashboard-action{flex:1;justify-content:center}}
    </style>
    <script>
        function dashboardAttendance(){return{busy:false,clockedIn:false,error:null,clockInAt:null,elapsed:'—',branchName:@js(auth()->user()->primaryBranch?->name??'Primary branch'),get clockInTime(){return this.clockInAt?new Date(this.clockInAt).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'}):'—'},async loadStatus(){try{const response=await fetch('{{ route('attendance.status') }}',{headers:{Accept:'application/json'}});if(!response.ok)return;const data=await response.json();this.clockedIn=!!data.clocked_in;this.clockInAt=data.attendance?.clock_in_at||null;this.branchName=data.attendance?.branch?.name||this.branchName;this.updateElapsed()}catch(e){}},updateElapsed(){if(!this.clockedIn||!this.clockInAt){this.elapsed='—';return}const seconds=Math.max(0,Math.floor((Date.now()-new Date(this.clockInAt).getTime())/1000));const h=Math.floor(seconds/3600),m=Math.floor(seconds%3600/60);this.elapsed=`${String(h).padStart(2,'0')}h ${String(m).padStart(2,'0')}m`;setTimeout(()=>this.updateElapsed(),60000)},submit(action){this.busy=true;this.error=null;if(!navigator.geolocation){this.error='Your browser does not support location services.';this.busy=false;return}navigator.geolocation.getCurrentPosition(position=>this.send(action,position),error=>{this.error=error.code===1?'Location permission was denied.':'Unable to determine your location. Please try again.';this.busy=false},{enableHighAccuracy:true,timeout:15000,maximumAge:0})},async send(action,position){try{const response=await fetch(`/attendance/${action}`,{method:'POST',headers:{'Content-Type':'application/json',Accept:'application/json','X-CSRF-TOKEN':document.querySelector('meta[name="csrf-token"]')?.getAttribute('content')||''},body:JSON.stringify({latitude:position.coords.latitude,longitude:position.coords.longitude,accuracy:position.coords.accuracy})});const data=await response.json().catch(()=>({}));if(!response.ok){this.error=data.message||'Attendance action failed.'}else{await this.loadStatus()}}catch(e){this.error='Unable to complete the attendance action. Please try again.'}finally{this.busy=false}}}}
    </script>
</x-filament-panels::page>
